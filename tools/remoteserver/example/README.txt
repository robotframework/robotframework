Remote library examples
'''''''''''''''''''''''

To use remote libraries with Python/Ruby, module search path must be configured
to contain corresponding remote server. After that the libraries can be
started with commands below::

  python examplelibrary.py
  ruby examplelibrary.rb

After a library has been started, the example test case file can be run normally::

  pybot example.html

