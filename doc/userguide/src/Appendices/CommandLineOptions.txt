All command line options
------------------------

This appendix lists all the command line options that are available
when `executing test cases`__ with :prog:`pybot` or :prog:`jybot`, and when 
`post-processing outputs`_ with :prog:`rebot`.

__ `Starting test execution`_

.. contents::
   :depth: 2
   :local:


Command line options for test execution
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~

  -N, --name <name>       `Sets the name`_ of the top-level test suite.
  -D, --doc <document>    `Sets the documentation`_ of the top-level test suite.
  -M, --metadata <name:value>  `Sets free metadata`_ for the top level test suite. 
  -G, --settag <tag>      `Sets the given tag(s)`_ to all executed test cases. 
  -t, --test <name>       Selects the executed `test cases by name`_.
  -s, --suite <name>      `Selects the test suites`_ to run by name. 
  -i, --include <tag>     `Selects the test cases`_ to run by tag. 
  -e, --exclude <tag>     `Selects the test cases`_ not to run by tag. 
  -c, --critical <tag>    Tests that have the given tag are `considered critical`_. 
  -n, --noncritical <tag>  Tests that have the given tag are `not critical`_. 
  --runmode <mode>        `Sets the execution mode`_ for this test run.
  -v, --variable <name:value>   `Sets variables`_ in the test data. 
  -V, --variablefile <path>  A file to `read variables`_ from. 
  -d, --outputdir <dir>   Defines where to `create output files`_.
  -o, --output <file>     Sets the path to the generated `output file`_.
  -l, --log <file>        Sets the path to the generated `log file`_. 
  -r, --report <file>     Sets the path to the generated `report file`_. 
  -S, --summary <file>    Sets the path to the generated `summary file`_. 
  -b, --debugfile <file>  The `debug file`_ that is written during execution. 
  -T, --timestampoutputs  `Adds a timestamp`_ in a format "YYYYMMDD-hhmmss" to all 
                          output file names between their basename and extension.
  --splitoutputs <level>  `Splits output and log`_ files from a specified suite level. 
  --logtitle <title>      `Sets a title`_ for the generated test log. 
  --reporttitle <title>   `Sets a title`_ for the generated test report. 
  --summarytitle <title>  `Sets a title`_ for the generated summary report.
  -L, --loglevel <level>  `Sets the threshold level`_ for logging. 
  --suitestatlevel <level>  Defines how many `levels to show`_ in the Statistics 
                            by Suite table in outputs. 
  --tagstatinclude <tag>  `Includes only these tags`_ in the Statistics by Tag and
                          Test Details by Tag tables in outputs. 
  --tagstatexclude <tag>  `Excludes these tags`_ from the Statistics by Tag and
                          Test Details by Tag tables in outputs.
  --tagstatcombine <tags>  Creates `combined statistics based on tags`_.
  --tagdoc <pattern:doc>   Adds `documentation to specified tags`_. 
  --tagstatlink <pattern:link:title>   Adds `external links`_ to the Statistics by 
                          Tag table in outputs.
  --listener <class>      A class for `monitoring test execution`_. 
  -W, --monitorwidth <chars>  `Sets the width`_ of the monitor output. 
  -C, --monitorcolors <on|off|force>  Using ANSI colors in the `console`_. 
  -P, --pythonpath <path>   Additional locations (directories, ZIPs, JARs) where
                          to `search test libraries`_ from when they are imported.
  -E, --escape <what:with>   `Escapes characters`_ that are problematic in the console.
  -A, --argumentfile <path>   The text file to `read more arguments`_ from. 
  -h, --help              Prints `usage instructions`_.
  --version               Prints the `version information`_.

Environment Variables:

ROBOT_SYSLOG_FILE         The path to the syslog file. If not specified, or set to
                          the special value "NONE", writing to syslog file is 
                          disabled. The path must be absolute. 
ROBOT_SYSLOG_LEVEL        The log level to use when writing to the syslog file.
                          The available levels are the same as for the  --loglevel
                          option. The default is INFO.



Command line options for post-processing outputs
~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~


Options:

  -N, --name <name>       `Sets the name`_ of the top level test suite. 
  -D, --doc <document>    `Sets the documentation`_ of the top-level test suite.
                          Underscores in the document are turned into spaces.
  -M, --metadata <name:value>  `Sets free metadata`_ for the top-level test suite.
  -G, --settag <tag>      `Sets the given tag(s)`_ to all processed test cases. 
  -t, --test <name>       Selects the `test cases by name`_. 
  -s, --suite <name>      `Selects the test suites`_ by name.
  -i, --include <tag>     `Selects the test cases`_ by tag.
  -e, --exclude <tag>     `Selects the test cases`_ to be excluded by tag.
  -c, --critical <tag>    Tests that have the given tag are `considered critical`_. 
  -n, --noncritical <tag>  Tests that have the given tag are `not critical`_.
  -d, --outputdir <dir>   Defines where to `create output files`_.
  -o, --output <file>     The XML `output file`_.
  -l, --log <file>        The HTML `log file`_.
  -r, --report <file>     The HTML `report file`_.
  -S, --summary <file>    The HTML `summary file`_.
  -T, --timestampoutputs  `Adds a timestamp`_ in a format "YYYYMMDD-hhmmss" to all
                          output file names between their basename and extension.
  --splitoutputs <level>  `Splits output and log`_ files from a specified suite 
                          level.
  --logtitle <title>    `Sets a title`_ for the generated test log.
  --reporttitle <title>  `Sets a title`_ for the generated test report.
  --summarytitle <title>  `Sets a title`_ for the generated summary report.
  --suitestatlevel <level>  Defines how many `levels to show`_ in the Statistics by Suite
                          table in outputs.
  --tagstatinclude <tag>  `Includes only these tags`_ in the Statistics by Tag and
                          Test Details by Tag tables in outputs.
  --tagstatexclude <tag>  `Excludes these tags`_ from the Statistics by Tag and
                          Test Details by Tag tables in outputs.
  --tagstatcombine <tags>  Creates `combined statistics based on tags`_.
  --tagdoc <pattern:doc>  Adds `documentation to specified tags`_.
  --tagstatlink <pattern:link:title>  Adds `external links`_ into the Statistics by Tag 
                          table in outputs.
  --removekeywords <all|passed>  `Removes keyword data`_ from the generated outputs. 
  --starttime <timestamp>  Sets the `starting time`_ of test execution when creating
                          combined reports.
  --endtime <timestamp>   The same as --starttime, but for `ending time`_.
  -E, --escape <what:with>  `Escapes characters`_ that are problematic in the console.
  -A, --argumentfile <path>   The text file to `read more arguments`_ from.
  -h, --help              Prints `usage instructions`_.
  --version               Prints the `version information`_.


Environment Variables:

ROBOT_SYSLOG_FILE         The path to the syslog file. If not specified, or set to
                          a special value "NONE", writing to syslog file is 
                          disabled. The path must be absolute. 
ROBOT_SYSLOG_LEVEL        The log level to use when writing to the syslog file.
                          The available levels are the same as for the --loglevel 
                          option to Robot and the default is INFO.


.. _Sets the name: `Setting the name`_
.. _Sets the documentation: `Setting the documentation`_
.. _Sets free metadata: `Setting free metadata`_
.. _Sets the given tag(s): `Setting tags`_
.. _test cases by name: `By test suite and test case names`_
.. _Selects the test suites: `test cases by name`_
.. _Selects the test cases: `By tag names`_
.. _considered critical: `Setting criticality`_
.. _not critical: `considered critical`_
.. _Sets the execution mode: `Setting the execution mode`_
.. _Sets variables: `Setting variables`_
.. _read variables: `Sets variables`_

.. _create output files: `Output directory`_
.. _Adds a timestamp: `Timestamping output files`_
.. _Splits output and log: `Splitting outputs`_
.. _Sets a title: `Setting titles`_

.. _Sets the threshold level: `Setting log level`_
.. _levels to show: `Configuring the Statistics by Suite table`_
.. _Includes only these tags: `Including and excluding tag statistics`_
.. _Excludes these tags: `Includes only these tags`_
.. _combined statistics based on tags: `Generating combined tag statistics`_
.. _documentation to specified tags: `Adding documentation to tags`_
.. _external links: `Creating links from tag names`_

.. _monitoring test execution: `Setting listeners`_
.. _Sets the width: `Controlling the monitor output`_
.. _console: `Sets the width`_
.. _search test libraries: `library search path`_
.. _Escapes characters: `Escaping options`_
.. _read more arguments: `Using argument files`_
.. _usage instructions: `Getting help on the command line`_
.. _version information: `usage instructions`_

.. _Removes keyword data: `Removing keywords from outputs`_
.. _starting time: `Setting times for combined outputs`_
.. _ending time: `starting time`_


